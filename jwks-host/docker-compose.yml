version: '3.8'

services:
  jwks-server:
    image: nginx:latest
    container_name: jwks-host
    ports:
      - "6060:80"
    volumes:
      - ./jwks:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: always
